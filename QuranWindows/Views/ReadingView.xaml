<UserControl
    x:Class="QuranWindows.Views.ReadingView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:QuranWindows.Models"
    mc:Ignorable="d">

    <Grid>
        <ListView ItemsSource="{Binding Verses}" SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:Verse">
                    <StackPanel Margin="0,10,0,10">
                        <TextBlock Text="{x:Bind VerseKey}" Foreground="Gray" FontSize="12"/>
                        <TextBlock Text="{x:Bind TextUthmani}" 
                                   FontSize="24" 
                                   FontFamily="Segoe UI" 
                                   TextWrapping="Wrap" 
                                   HorizontalAlignment="Right" 
                                   TextAlignment="Right"
                                   Margin="0,5,0,0"/>
                        <MenuFlyoutSeparator Margin="0,15,0,5"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        
        <ProgressRing IsActive="{Binding IsLoading}" 
                      HorizontalAlignment="Center" 
                      VerticalAlignment="Center" 
                      Width="50" Height="50"/>
        
        <TextBlock Text="{Binding ErrorMessage}" 
                   Foreground="Red" 
                   HorizontalAlignment="Center" 
                   VerticalAlignment="Bottom" 
                   Margin="20"
                   Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}, FallbackValue=Collapsed}"/>
    </Grid>
</UserControl>
